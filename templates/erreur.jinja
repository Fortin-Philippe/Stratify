{% extends "base.jinja" %}

{% block title %} - Erreur {{ code }}{% endblock %}

{% block content %}
<div class="text-center my-5">
    <h1 class="display-4 text-danger">Erreur {{ code }}</h1>
    <p class="lead">{{ message }}</p>
    <a href="{{ url_for('home') }}" class="btn btn-primary mt-3">
        Retour à l’accueil
    </a>
</div>
{% endblock %}