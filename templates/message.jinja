{% extends 'base.jinja' %}

{% block content %}
<div class="forum-container">
    <form method="POST" class="discussion-form">
        <input type="hidden" name="auteur" value="{{ user_name }}">
        
        <div class="form-info">
            <p>Publié par : <strong>{{ user_name }}</strong></p>
        </div>

        <div class="form-group">
            <label for="categorie">Catégorie *</label>
            <select id="categorie" name="categorie" required>
                <option value="discussion">Discussion</option>
                <option value="question">Question</option>
                <option value="guide">Guide</option>
            </select>
        </div>

        <div class="form-group">
            <label for="titre">Titre de la discussion *</label>
            <input type="text" id="titre" name="titre" required maxlength="200" 
                   placeholder="Ex: Comment améliorer mon CS ?">
        </div>

        <div class="form-group">
            <label for="contenu">Contenu *</label>
            <textarea id="contenu" name="contenu" required rows="10" 
                      placeholder="Décrivez votre question ou sujet en détail..."></textarea>
        </div>

        <div class="form-actions">
            <a href="{{ url_for('forum.index') }}" class="btn btn-secondary">Annuler</a>
            <button type="submit" class="btn btn-primary">Publier la discussion</button>
        </div>
    </form>
</div>
{% endblock %}